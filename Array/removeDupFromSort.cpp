#include<bits/stdc++.h>
using namespace std;

vector<int> removeDup(vector<int> &nums){
    int n = nums.size();

    
}

int main() {
    int n ;
    cout << "Enter size: ";
    cin >> n;

    vector<int>arr(n);
    for(int i = 0; i < n; i++){
        cin >> arr[i];
    }
}